# Insta Unlike Bot

Quick start:

1. Install Python 3.
2. Open a terminal in this folder and run:
   `pip install -r requirements.txt`
3. Create a file named `.env` next to `bot.py` and add:
   `IG_USERNAME=your_username`
   `IG_PASSWORD=your_password`
4. Run:
   `python bot.py`
